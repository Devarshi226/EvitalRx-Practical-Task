import { FormateIndianCurrencyPipe } from './formate-indian-currency.pipe';

describe('FormateIndianCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateIndianCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
